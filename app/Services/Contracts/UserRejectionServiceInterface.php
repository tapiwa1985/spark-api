<?php

namespace App\Services\Contracts;

/**
 * Contract for handling user rejection logic.
 *
 * This interface extends the base service contract and is intended
 * to define methods related to managing user rejections (e.g., rejecting
 * content, matches, or interactions). Currently, no additional methods
 * are required beyond those provided by the base interface.
 *
 * @package App\Services\Contracts
 */
interface UserRejectionServiceInterface extends BaseServiceInterface
{
}
